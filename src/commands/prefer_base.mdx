import { SlashCommandBuilder, ChatInputCommandInteraction, TextChannel, DMChannel, NewsChannel, ThreadChannel, PermissionFlagsBits } from 'discord.js';
import type { Command } from '../../types/index';

export const command: Command = {
    data: new SlashCommandBuilder()
        .setName('name')
        .setDescription('Descripcion')
        .setDMPermission(false) // Permisos para el DM
        .setDefaultMemberPermissions(PermissionFlagsBits.Administrator) // Definir permisos por defecto ( Administrator )
        // Agregar otra opción
        .addStringOption(option =>
            option.setName('name')
                .setDescription('descripction')
                .setRequired(true) // Si es requerido a que lo pongo
        ),

        async execute(client, interaction: ChatInputCommandInteraction) {



            
        }

}
